class Image {
  private bookName: string;
  private file: string;

  constructor(
    bookName: string,
    file: string,
  ) {
    this.bookName = bookName;
    this.file = file;

  }
}

export default Image;