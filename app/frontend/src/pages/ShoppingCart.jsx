import React from "react";
import { Container, Row } from 'react-bootstrap';

const ShoppingCart = ({products}) => {
  console.log('Entrei com alguns', products);
  return (
    <Container>
      <Row>

      </Row>
    </Container>
  )
};

export default ShoppingCart;